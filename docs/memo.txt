z_current/order_tsptw.py で定式化されるQUBO問題の新しい定式化を考えています。
各頂点について離散時間を考え、各時間で頂点が独立した別のものとします。
このとき、同一の頂点の別の時間における重複訪問にペナルティを課すことで、別の頂点に分割した問題を解消します。
時間を離散的に分割する方法を以下に示します。
各頂点はtime window [l,r)を持ちます。
このときすべてのl,rを一つの配列にまとめてsort,uniqueし、イベントの起きる時間を唯一で抽出します。
各イベント時間について、time windowが有効な頂点を列挙します。
1イベントごとの列挙はO(klog N)時間、合計でO(N klog N)時間でできるはずです。kはそのイベント時間で有効なタイムウィンドウを持つ頂点の数です。
このとき、ある時間tにおける頂点vに訪問したことを示すバイナリ変数をx_{tv}で表現し、qubo定式化できませんか。
距離と前後関係をどのように扱うかという点を明確にしたいです。

z_current/tsptw_plot.pyを参考にしてtime_tsptw.pyに描画するプログラムを追加してください。

time_tsptw.pyがどのような定式化か詳しく説明してください。
またこの定式化の残っている課題を教えてください。

課題1の解決策を考えてください。

src/order_tsptw.pyでは以下のようなバイナリ変数を使ってTSP with time windowを定式化しています。
x[i][u] = 1 <=> i番目に顧客uを訪れる
ソースコードを調べて、修正点、改善点を見つけてnew_order_tsptw.pyに保存してください。
保存ができたら実行はせずに修正箇所を説明してください。

今のnew_order_tsptw.pyでは以下のようなエラーが起きてしまいます。修正してください。
  File "/home/kazuki/qbpp/src/new_order_tsptw.py", line 295, in main
    row_constraint += qbpp.cons(row_sums[i], equal=1)
                      ~~~~~~~~~^^^^^^^^^^^^^^^^^^^^^^
  File "/home/kazuki/qbpp/.venv/lib/python3.14/site-packages/pyqbpp/_nb_dispatch.py", line 518, in cons
    raise TypeError(
        f"cons(): expected an expression body, got {type(e)}")
TypeError: cons(): expected an expression body, got <class 'pyqbpp_nb._nb_c32e64m0._ExprRef'>

src/new_order_tsptw.pyとsrc/time_tsptw_travelはどちらもTSP with time windowをQUBOで定式化しているソースコードです。
違いは、以下のバイナリ変数の表現です。
src/new_order_tsptw.py：x[i][u]=1 <=> i番目に顧客uを訪れる
src/time_tsptw_travel.py：x[t][u]=1 <=> 時刻tに顧客uを訪れる
この二つの定式化をDumasのインスタンスを使って、比較したいです。
docs/tsptw_result.mdに結果を保存してください。表形式で比較してください。どのような書き方でも構いません。

new_tsptw.pyは以下のような意味を持つバイナリ変数を用いてTSP with time windowを定式化しています。
x[i][u]=1 <=> i番目に顧客uを訪れる
変数数、項数は少ない定式化ですが、ほとんどの実行でone-hot制約が破られてしまいます。
この問題を解決するためにいくつかの方法を考えました。それらの方法で最も良いと考えられるものを新たに、
improved_new_tsptw.pyとして保存し、説明してください。
・one-hot制約をより強くするため冗長ではあるが似たような制約を追加する
・ペナルティ係数を最適化し、解の質が向上することを願う
・ほかに案があれば追加し、比較してください

time_travel.pyはTSP with time windowをpyQUBO++を用いてQUBOで定式化しています。定式化には以下のバイナリ変数を用いています。
x[t][u]=1 <=> 時刻tに顧客に訪問する。
この定式化は実行時間を長くすると、best-known solutionを得ることができる程度の優秀さの定式化です。
TSP with time windowの論文を大量に調べて、良い結果が出ている定式化を見つけてきてください。
定式化の方法は問いません。QUBOでも、HUBOでも、MILPでも、gurobiでも構いません。
その中で、time_travel.pyと同じような定式化があるかも調べてください。
探す論文はarxivなどにある、未査読のものでも構いません。

研究室にある共用GPUはこのPCからしか接続できません。(ノートPCからの接続ができない)
なので、このPCとノートPCをsshで接続し、ノートPCからでもGPUに接続できるようにしたいです。
ノートPC -ssh-> デスクトップPC -ssh-> GPU
wsl間で接続できれば良いのですが、powershell経由でも構いません。
ノートPC側での準備は私がやるので、このPCでの環境構築をお願いします。

time_travel.pyの定式化を説明してください。特に制約Bがわからないので詳しく説明してください。
また、time_makespan.pyとの違いも説明してください。

conflict_termsで(x,x),(x,b),(b,x),(b,b)を禁止していますが、訪問前に待機、または、訪問後に待機に固定することで、組を減らすことはできないのですか？
例：uからvへの移動の時、早着してしまうならuに訪問後、uに待機してからvへ向かう。これをすべての顧客で統一する。もしくは先にvに到着しておいてから待機して訪問する。に統一する。

bを使わずに表す方法を一つ思いつきました。
x[t][u]=x[t+1][u]=1となっているなら待機時間+1して、x[t+k][u]=0となれば、それまでに足した待機時刻がuでの待機時刻になると思います。
ただ、定式化の方法を思いつかず、また、QUBOの形で表せるかもわかりません。

time_travel.pyやtime_makespan.py等で使われている、conflict_termですが、現在インスタンスとして使っているDumasインスタンスは必ずc(u,v) = c(v,u)となっています。なので、逆向きの操作は不要である。という結論が別のセッションで説明されていました。
実際に確認して、減らすことでのメリット、デメリットを提示してください。

https://www.media.hiroshima-u.ac.jp/services/hinet/
こちらのサイトのHINETというネットワークが広島大学では使われています。
研究室のPCはこちらに接続しているのですが、毎日ログインが必要です。また、翌日AM4時に自動的に接続が切れるようになっています。この影響で、数日経って学外からこのコンピュータを動かすことができません。

現在、あなたの出力が終了するとPCに通知が来るように設定しています。これをiPhoneにも通知が来るようにしたいです。可能な場合はこのPC側の設定をお願いします。また、iPhone側はこちらで設定しますが、設定すべきことは伝えてください。

今は、「処理が完了しました！」という文に固定されていますが、そちら側で出力の内容を自動的に要約して通知することは可能ですか？

time_travel.pyでn60w100を変数数、項数を確認して現実的であれば、実行してください。シードを振ることが重要という結論が別セッションで出ていたので、時間を長くとるより、シード変更し、複数回実行してみてください。

time_occupancy.pyはtime_travel.pyと同値でしょうか？だとしたら、比較は意味がないかもしれませんが、気になります。同値であるかどうかの確認をお願いします。実行するかの判断はその結果に応じてこちらでするので、まずは確認だけお願いします。

time_occupancy.pyとtime_travel.py、人間の理解しやすさを考えて、time_occupancy.pyを今後使用しようと考えていますが、いいと思いますか？

time_occupancy.pyは時間幅が大きくなればなるほど解くのが難しくなります。なので、シンプルなTSPに適用しようとしたり、各顧客のtime windowが大きいとorder型のほうが有利に働くことがあります。
この弱点を克服するために何か案はありますか？私は時間全体をいくつかのブロックに分割して解く方法が思いつきましたが、最適解が失われるという欠点が残りました。
どのように考えれば、この問題を克服できるでしょうか？

time_occupancy.pyの定式化をmTSPTWに応用しようと考えています。まだ具体的なことは決まっていませんが、変数を以下のようにしようと考えています。
x[t][i][v]=1 <=> 時刻tに顧客iに車両vが訪れる。
制約A：\sum_t\sum_v x[t][i][v] = 1 \forall i
制約B：\sum_{両立しない組}x[t][i][v]x[t'][i'][v]
目的関数：各車両の移動時間の最大値の最小化
このままでは、制約が足りない気がしています。この二つの制約だけだと対応できない例を教えてください。制約は自分で作りたいので例だけを教えてください。
また、もし制約A,制約Bが間違えているならそれも教えてください。

time_occupancy.pyの変数をx[t][i][v]に変更した、time_occupancy_multi.pyを作成してください。
以下の注意点を読んでおいてください。
・制約A,Bを新たな変数に対応付けてください
・制約の追加は行わないでください。
・目的関数もそのままで大丈夫です。(解きたい問題と合致していなくて構いません。)
・移植すると壊れる仕掛けは壊れないようm台に対応できるようにしてください。
・軽いインスタンスを使って、問題がないかの確認をしてください。